<div class="container">

<h1>Sample Rails ERB App</h1>

<div class="alert alert-info">
This is a live CRUB Ruby on Rails app. For demo purposes, this is like Foreman/Katello.
</div>

<table class="table table-bordered table-striped">
  <tr>
    <th>Name</th>
    <th>Url</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.name %></td>
    <td><%= blog.url %></td>
    <td><%= link_to 'Show', blog %></td>
    <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
    <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Blog', new_blog_path, :class => 'btn btn-default btn-small' %>

</div>